# Recorded naming-rule exceptions (spec §5.3). Every record needs a
# rule id, a reason, an owner, and the condition that reopens it.
# Generated by `pika adopt`; each decision reviewed and written by hand.

internal/discover/testdata/swift-xcode/MyApp.xcodeproj/project.pbxproj:
  rule-id: naming-kebab-case
  path: internal/discover/testdata/swift-xcode/MyApp.xcodeproj/project.pbxproj
  reason: 'Xcode fixture: internal/discover detects a Swift project by the exact "*.xcodeproj/project.pbxproj" layout Xcode writes, so the fixture must reproduce those names verbatim. Renaming it would make the test stop exercising the detector it exists to cover.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if Xcode stops writing .xcodeproj/project.pbxproj, or if the Swift detector stops keying on that path.'
internal/initcmd/templates/Cargo.toml.tmpl:
  rule-id: naming-kebab-case
  path: internal/initcmd/templates/Cargo.toml.tmpl
  reason: 'template names mirror the file they emit, and Cargo requires the manifest to be named exactly "Cargo.toml". The rule already exempts the "Cargo" stem for the emitted file; the ".tmpl" suffix is what defeats the single-extension stem strip, not the name.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if the templates stop being named for the file they emit, or if the emitted file''s name stops being fixed by convention.'
internal/initcmd/templates/Package.swift.tmpl:
  rule-id: naming-kebab-case
  path: internal/initcmd/templates/Package.swift.tmpl
  reason: 'template names mirror the file they emit, and SwiftPM requires the manifest to be named exactly "Package.swift". The rule already exempts the "Package" stem for the emitted file; the ".tmpl" suffix is what defeats the single-extension stem strip, not the name.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if the templates stop being named for the file they emit, or if the emitted file''s name stops being fixed by convention.'
internal/profiles/packs/core@1.yaml:
  rule-id: naming-kebab-case
  path: internal/profiles/packs/core@1.yaml
  reason: 'the file name is the pack identifier: "<profile>@<version>" is the exact ref a contract writes under profiles: and the key the embedded registry resolves, so the offending "@" carries meaning. Renaming the file would insert a translation table between a contract ref and the pack it names.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if pack refs stop using "@" as the version separator, or if the registry stops resolving a ref to a file of the same name.'
internal/profiles/packs/core@1/templates/AGENTS.md.tmpl:
  rule-id: naming-kebab-case
  path: internal/profiles/packs/core@1/templates/AGENTS.md.tmpl
  reason: 'the offending segments are the "core@1" pack directory (the pack identifier) and the stem "AGENTS.md", which mirrors the file this template emits. core@1 itself requires the emitted file be named AGENTS.md. The rule already exempts the "AGENTS" stem for the emitted file; the ".tmpl" suffix is what defeats the single-extension stem strip, not the name.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if the templates stop being named for the file they emit, or if the emitted file''s name stops being fixed by convention.'
internal/profiles/packs/core@1/templates/CONTRIBUTING.md.tmpl:
  rule-id: naming-kebab-case
  path: internal/profiles/packs/core@1/templates/CONTRIBUTING.md.tmpl
  reason: 'the offending segments are the "core@1" pack directory (the pack identifier) and the stem "CONTRIBUTING.md", which mirrors the file this template emits. core@1 itself requires the emitted file be named CONTRIBUTING.md. The rule already exempts the "CONTRIBUTING" stem for the emitted file; the ".tmpl" suffix is what defeats the single-extension stem strip, not the name.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if the templates stop being named for the file they emit, or if the emitted file''s name stops being fixed by convention.'
internal/profiles/packs/core@1/templates/README.md.tmpl:
  rule-id: naming-kebab-case
  path: internal/profiles/packs/core@1/templates/README.md.tmpl
  reason: 'the offending segments are the "core@1" pack directory (the pack identifier) and the stem "README.md", which mirrors the file this template emits. core@1 itself requires the emitted file be named README.md. The rule already exempts the "README" stem for the emitted file; the ".tmpl" suffix is what defeats the single-extension stem strip, not the name.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if the templates stop being named for the file they emit, or if the emitted file''s name stops being fixed by convention.'
internal/profiles/packs/core@1/templates/ci.yml.tmpl:
  rule-id: naming-kebab-case
  path: internal/profiles/packs/core@1/templates/ci.yml.tmpl
  reason: 'the offending segment is the "core@1" pack directory, whose name is the pack identifier (see internal/profiles/packs/core@1.yaml); the template file itself conforms.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if pack refs stop using "@" as the version separator, or if the registry stops resolving a ref to a file of the same name.'
internal/profiles/packs/core@1/templates/pull_request_template.md.tmpl:
  rule-id: naming-kebab-case
  path: internal/profiles/packs/core@1/templates/pull_request_template.md.tmpl
  reason: 'the offending segment is the "core@1" pack directory, whose name is the pack identifier (see internal/profiles/packs/core@1.yaml); the template file itself conforms.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if pack refs stop using "@" as the version separator, or if the registry stops resolving a ref to a file of the same name.'
internal/profiles/packs/go@1.yaml:
  rule-id: naming-kebab-case
  path: internal/profiles/packs/go@1.yaml
  reason: 'the file name is the pack identifier: "<profile>@<version>" is the exact ref a contract writes under profiles: and the key the embedded registry resolves, so the offending "@" carries meaning. Renaming the file would insert a translation table between a contract ref and the pack it names.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if pack refs stop using "@" as the version separator, or if the registry stops resolving a ref to a file of the same name.'
internal/profiles/packs/python@1.yaml:
  rule-id: naming-kebab-case
  path: internal/profiles/packs/python@1.yaml
  reason: 'the file name is the pack identifier: "<profile>@<version>" is the exact ref a contract writes under profiles: and the key the embedded registry resolves, so the offending "@" carries meaning. Renaming the file would insert a translation table between a contract ref and the pack it names.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if pack refs stop using "@" as the version separator, or if the registry stops resolving a ref to a file of the same name.'
internal/profiles/packs/rust@1.yaml:
  rule-id: naming-kebab-case
  path: internal/profiles/packs/rust@1.yaml
  reason: 'the file name is the pack identifier: "<profile>@<version>" is the exact ref a contract writes under profiles: and the key the embedded registry resolves, so the offending "@" carries meaning. Renaming the file would insert a translation table between a contract ref and the pack it names.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if pack refs stop using "@" as the version separator, or if the registry stops resolving a ref to a file of the same name.'
internal/profiles/packs/swift@1.yaml:
  rule-id: naming-kebab-case
  path: internal/profiles/packs/swift@1.yaml
  reason: 'the file name is the pack identifier: "<profile>@<version>" is the exact ref a contract writes under profiles: and the key the embedded registry resolves, so the offending "@" carries meaning. Renaming the file would insert a translation table between a contract ref and the pack it names.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if pack refs stop using "@" as the version separator, or if the registry stops resolving a ref to a file of the same name.'
internal/profiles/packs/typescript@1.yaml:
  rule-id: naming-kebab-case
  path: internal/profiles/packs/typescript@1.yaml
  reason: 'the file name is the pack identifier: "<profile>@<version>" is the exact ref a contract writes under profiles: and the key the embedded registry resolves, so the offending "@" carries meaning. Renaming the file would insert a translation table between a contract ref and the pack it names.'
  owner: '@Choaterboater (pika kernel maintainer)'
  review-condition: 'reopen if pack refs stop using "@" as the version separator, or if the registry stops resolving a ref to a file of the same name.'
