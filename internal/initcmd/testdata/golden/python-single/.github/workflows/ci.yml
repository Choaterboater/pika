name: check
on:
  push:
    branches: [main]
    paths:
      - '.project/**'
      - 'docs/**'
      - 'AGENTS.md'
      - 'README.md'
      - 'CONTRIBUTING.md'
      - '.github/workflows/ci.yml'
      - 'src/**'
      - 'tests/**'
      - 'pyproject.toml'
  pull_request:
    paths:
      - '.project/**'
      - 'docs/**'
      - 'AGENTS.md'
      - 'README.md'
      - 'CONTRIBUTING.md'
      - '.github/workflows/ci.yml'
      - 'src/**'
      - 'tests/**'
      - 'pyproject.toml'
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install test tooling
        run: python -m pip install pytest
      - name: Install projectctl
        run: go install github.com/Choaterboater/projectctl/cmd/projectctl@latest
      - name: Verify
        run: projectctl check --ci
